// This file intentionally left as a no-op route shim to prevent expo-router from auto-mounting the real service.
// The real background sync service is located at `services/syncService.js` (project root).

export default null;
